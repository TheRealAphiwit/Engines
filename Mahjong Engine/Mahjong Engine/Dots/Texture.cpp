#include "Texture.h"
// #include "stb_image.h"
#include <glad/glad.h>
#include <glfw3.h>
#include <iostream>

Texture::Texture(const char* path, const bool& shouldAlpha)
{
	int Channels = 0;
	Width = 0;
	Height = 0;

	// unsigned char* data = stbi_load();
}